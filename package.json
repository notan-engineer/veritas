{
  "name": "veritas",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cd services/ui && npm run dev",
    "build": "cd services/ui && npm run build",
    "start": "cd services/ui && npm run start",
    "lint": "cd services/ui && npm run lint",
    "test:env": "cd services/ui && node -e \"console.log('Environment check passed')\"",
    "test:smoke": "cd services/ui && npm run build"
  },
  "devDependencies": {
    "@railway/cli": "^3.0.0"
  },
  "dependencies": {
    "@types/pg": "^8.15.4",
    "pg": "^8.16.3"
  }
}
